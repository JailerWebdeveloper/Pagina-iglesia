---
const {imagen ,titulo , autor , fecha , descripcion} = Astro.props
---
<div class="p-4 hover:bg-gray-400 transition duration-350 ease-in-out">
<div class="flex flex-col gap-1 items-start">
    <img src={imagen} alt="Loading..." class="w-full h-56 rounded-none">
    <p class="text-primary break-all font-semibold text-start hover:text-gray-600 text-lg">{titulo}</p>
    <p class="font-bold text-sm text-gray-800 capitalize">{autor}</p>
    <p>{fecha}</p>
    <p class="my-2 text-gray-700 text-justify break-all">{descripcion}</p>
</div>
</div>